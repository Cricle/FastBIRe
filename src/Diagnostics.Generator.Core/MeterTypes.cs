namespace Diagnostics.Generator.Core
{
    public enum MeterTypes
    {
        Counter = 0,
        Histogram = 1,
        ObservableGauge = 2,
        ObservableUpDownCounter = 3,
        UpDownCounter = 4
    }
}
